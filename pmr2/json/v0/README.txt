Legacy pmr2.json module, v0
===========================

This is the migrated location for legacy code from the first version,
moved to a form where it is still functional with the old Accept header
for backward compatibility.

One thing to note is that ``document`` view was not implemented.
